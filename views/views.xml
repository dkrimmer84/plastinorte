<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="plastinorte.list">
      <field name="name">plastinorte list</field>
      <field name="model">plastinorte.plastinorte</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="plastinorte.action_window">
      <field name="name">plastinorte window</field>
      <field name="res_model">plastinorte.plastinorte</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="plastinorte.action_server">
      <field name="name">plastinorte server</field>
      <field name="model_id" ref="model_plastinorte_plastinorte"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="plastinorte" id="plastinorte.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="plastinorte.menu_1" parent="plastinorte.menu_root"/>
    <menuitem name="Menu 2" id="plastinorte.menu_2" parent="plastinorte.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="plastinorte.menu_1_list" parent="plastinorte.menu_1"
              action="plastinorte.action_window"/>
    <menuitem name="Server to list" id="plastinorte" parent="plastinorte.menu_2"
              action="plastinorte.action_server"/>
    -->
  </data>
</openerp>